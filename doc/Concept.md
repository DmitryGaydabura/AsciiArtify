# Concept: Local Kubernetes for AsciiArtify (minikube vs kind vs k3d)

> **Prepared for:** AsciiArtify — ML-powered image→ASCII-art startup  
> **Date:** 2025-11-13

## Вступ
Мета — обрати інструмент для **локального** Kubernetes, щоб швидко розгортати/тестувати PoC. Розглянуто три опції:
- **minikube** — «локальний Kubernetes одним рядком», підтримує різні драйвери (Docker/Podman/VM).
- **kind** — Kubernetes **IN** Docker/Podman; кластери як контейнери.
- **k3d** — «k3s у Docker»: легкий дистрибутив Kubernetes (k3s) у контейнерах, дуже швидкий старт.

Також враховано ризики комерційного використання **Docker Desktop** та можливість альтернативи **Podman**.

## Характеристики

| Критерій | **minikube** | **kind** | **k3d** |
|---|---|---|---|
| Платформи | macOS, Linux, Windows | macOS, Linux, Windows | macOS, Linux, Windows |
| Підтримувані архітектури | amd64/arm64 | amd64/arm64 | amd64/arm64 |
| Рантайм/драйвер | Docker, VM (KVM/VirtualBox/QEMU), **Podman (experimental)** | Docker, **Podman (rootless підтримується)** | **Вимагає Docker** (контейнеризований k3s) |
| Швидкість старту | Середня (VM-драйвери повільніші) | **Швидко** | **Дуже швидко** |
| Мульти-нода | Підтримується | **Легко** (через node=контейнер) | **Легко** (k3s агенти як контейнери) |
| Автоматизація (CI) | `minikube start`, addons, profiles | Простий YAML для топологій, reproducible | `k3d cluster create` з параметрами |
| Аддони/UX | Багато аддонів (dashboard, ingress) | Мінімалістично, зате передбачувано | Мінімалістично, але зручні фічі (`registries`, `loadbalancer`) |
| Образи розробника | `minikube image build` або Docker локально | `kind load docker-image` / `build` | `k3d image import` |
| Моніторинг | addons (metrics-server, dashboard) | вручну через манифести/Helm | вручну або готові приклади |
| Сумісність з Podman | **Є (experimental)** | **Є (rootless/Podman)** | Офіційно **Docker only** |

## Переваги та недоліки

### minikube
**+** Багато драйверів (можна без Docker Desktop), аддони, learning‑friendly.  
**–** Подекуди повільніше; Podman‑драйвер досі менш зрілий; різні драйвери — різні «гострі кути».

### kind
**+** Кластери як контейнери ⇒ швидко, відтворювано; працює з Podman/rootless; ідеально для CI.  
**–** Менше «із коробки» аддонів; деякі сценарії з мережами на Windows/macOS потребують налаштувань.

### k3d
**+** Найшвидший «реальний» Kubernetes (k3s) локально; прості мульти‑нод/порт‑маппінг; легка вага.  
**–** Потребує **Docker**; з Podman — офіційно не підтримується; інколи відмінності k3s vs k8s‑«ваніль».

## Ліцензування Docker Desktop та альтернатива Podman
- **Docker Desktop** безкоштовний для персонального/освітнього використання і малих бізнесів (<250 співробітників **і** <$10M revenue). Для більшості комерційних команд — потрібна платна підписка.  
- **Podman** — бездемонний, open‑source, CLI сумісний із Docker, добре працює на Linux і має Podman Desktop на macOS/Windows.  
- Враховуючи стартап‑статус, можна почати з безкоштовного тиру; якщо компанія зросте понад ліміти — перейти на платний план або мігрувати на Podman.

## Рекомендація
Для PoC **AsciiArtify** рекомендуємо **k3d** як найшвидший і найзручніший шлях до мульти‑нод кластера, локального реєстру і швидких циклів розробки.  
Альтернатива без Docker Desktop — **kind + Podman** (rootless), або **minikube** з драйвером Docker/Podman залежно від ОС.

---

## ДЕМО (вбудоване): «Hello World» на рекомендованому **k3d**

### Передумови
- Встановлено: `docker`, `kubectl`, `k3d`
- Репозиторій із цією папкою `k8s/hello`

### Кроки
```bash
# 1) Створюємо кластер з локальним реєстром
k3d registry create k3d-registry.localhost --port 5001 || true
k3d cluster create asciiartify   --servers 1 --agents 2   --registry-use k3d-k3d-registry.localhost:5001   --port "8080:80@loadbalancer"

# 2) Неймспейс та деплой
kubectl create ns demo || true
kubectl -n demo apply -f k8s/hello/deployment.yaml
kubectl -n demo apply -f k8s/hello/service.yaml

# 3) Перевірка
kubectl -n demo get pods -o wide
kubectl -n demo get svc hello

# 4) Тест
curl -s http://localhost:8080/
```

### Очікувані результати (фрагмент консолі)
```
$ kubectl -n demo get svc hello
NAME    TYPE       CLUSTER-IP     EXTERNAL-IP   PORT(S)        AGE
hello   ClusterIP  10.43.122.231  <none>        80/TCP         8s

$ curl -s localhost:8080
hello from asciiartify 👋
```

> **Порада:** замініть образ у `Deployment` на ваш (локально збілдити та `k3d image import <image>`).

---

## ДЕМО для альтернатив

### kind (працює з Podman)
```bash
kind create cluster --name asciiartify
kubectl create ns demo || true
kubectl -n demo apply -f k8s/hello/deployment.yaml
kubectl -n demo apply -f k8s/hello/service.yaml
kubectl -n demo port-forward svc/hello 8080:80
curl -s localhost:8080
```

### minikube
```bash
# Оберіть драйвер: docker | podman | virtualbox | qemu | kvm2 (linux)
minikube start --driver=docker
kubectl create ns demo || true
kubectl -n demo apply -f k8s/hello/deployment.yaml
kubectl -n demo apply -f k8s/hello/service.yaml
minikube service hello -n demo --url
```

---

## Висновки
- **k3d** — оптимально для PoC: найшвидший старт, мульти‑нод за хвилини, простий порт‑форвардинг та локальний реєстр.  
- **kind** — чудово для CI та сценаріїв з **Podman**/rootless; мінімум «магії», максимально передбачувано.  
- **minikube** — найгнучкіший щодо драйверів та аддонів; хороший для навчання та демонстрацій, особливо без Docker Desktop.

**Рекомендація для AsciiArtify PoC:** починати з **k3d**, тримати готовий профіль під **kind+Podman**, мати «fall‑back» через **minikube** там, де потрібно більше аддонів або інший драйвер.
